<form method="post" action="{{ path('app_tricks_delete', {'name': trick.name}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce tricks?');">
    <input type="hidden" name="_token_delete" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button type="submit" class="mx-0 md:mx-3 my-2 flex w-full justify-center rounded-md bg-danger-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-danger-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary2 lg:hover:shadow-lg lg:hover:duration-500 lg:hover:shadow-danger-600 lg:hover:-translate-y-1.5">Supprimer</button>
</form>
